<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huiduw.xshopping.mapper.GoodsInfoMapper">
    <select id="findByName" resultType="com.huiduw.xshopping.model.GoodsInfo">
        select *,
        (select ty.name from types_info ty where ty.id=goods_info.typeId) typeName,
        (select u.name from user_info u where u.id=goods_info.userid) userName
        from goods_info
        where 1=1
        <if test="name != null and name != 'all' and name != ''">
            and goods_info.`name` like concat('%',#{name},'%')
        </if>
        <if test="id != null and id != ''">
            and goods_info.`id` = #{id}
        </if>
    </select>

</mapper>